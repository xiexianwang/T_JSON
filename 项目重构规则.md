# T-JSON 项目重构规则与实施指南 (工业级 DDD/MVP 架构)

## 1. 架构目标
打破现有单一 `MainWindow` 中的过度耦合，引入 **领域驱动设计 (DDD)**、**MVP (Model-View-Presenter) 模式** 及 **全局事件总线**，以满足工业级上位机的高可用性、可扩展性，并原生支持**多设备并行管理**。

---

## 2. 系统分层 (Layered Architecture)

所有代码文件必须按照以下层级进行划分，严格遵守**依赖传递原则（外层依赖内层，内层绝不依赖外层）**。

### 2.1 基础设施层 (Infrastructure)
*   **职责**: 直接面对底层的网络通信、硬件交互及第三方库封装，不含任何业务逻辑。
*   **包含组件**: 
    *   网络通信: `TJsonClient` (TCP), `SerialPortHandler`
    *   流媒体引擎: `RtspThread` (FFmpeg)
    *   外部对接: `S3Uploader` (AWS), 本地配置存储 `ConfigManager`
*   **规则**: 
    *   不允许 `#include` 任何 UI 相关的头文件 (`QWidget`, `MainWindow`)。
    *   发生数据到达或错误时，通过事件总线或回调机制向上层投递。

### 2.2 核心领域层 (Core/Domain)
*   **职责**: 存放系统的纯数据结构、抽象接口和工具算法。
*   **包含组件**:
    *   数据模型: `DeviceState` (存放云台角度、倍率、GPS、在线状态等所有状态数据)。
    *   基础算法: `GeoCalculator` (像素转GPS、目标距离估算等纯数学类)。
    *   **核心解耦枢纽**: `EventBus` (全局信号槽事件总线)。
*   **规则**:
    *   绝对不含平台强相关的库调用或 UI 逻辑。
    *   数据模型 (`DeviceState`) 只做数据存储，不自己发网络请求。

### 2.3 业务上下文层 (Services)
*   **职责**: 将基础设施与数据模型缝合在一起，形成业务处理流。
*   **包含组件**:
    *   `DeviceContext`: 表示**单台设备**的聚合根。内部封装自己的 `TJsonClient`、`RtspThread` 和 `DeviceState`。
    *   `DeviceManager`: 多设备管理的全局单例，维护 `QMap<QString, DeviceContext*>`。负责设备的增删改查。
    *   `DeviceController`: 负责具体的 PTZ、传感器控制指令分发。
*   **规则**:
    *   在 `DeviceContext` 内部完成自己所辖网络链路的数据收发闭环。
    *   当 `DeviceContext` 解析完一帧 JSON 并更新自己的 `DeviceState` 后，立即向全局 `EventBus` 发送广播 (如：`DeviceStateChangedEvent(deviceId)`)。

### 2.4 表现层 (UI/MVP)
*   **职责**: 只负责画界面的 `View`，和负责处理 UI 逻辑的 `Presenter`。
*   **包含组件**:
    *   `MainWindow` (纯视图): 剥离一切业务，变成“哑巴(Dumb) 组件”。只保留如 `updatePtzLabel()`, `showErrorDialog()` 这样的绘图函数。
    *   `MainPresenter` (控制器): 监听 `MainWindow` 抛出的按钮点击事件，调用 `DeviceManager`；同时监听 `EventBus`，并根据事件调用 `MainWindow` 刷新界面。
    *   多设备UI容器: `VideoGridWidget` (管理宫格)、`MapWidget` (显示多个设备的 marker)。
*   **规则**:
    *   **UI (View) 绝对不能直接调用网络层对象 (TJsonClient)！** 一切通过 Presenter 中转。

---

## 3. 核心解耦模式

### 3.1 跨模块通信：Global EventBus
放弃不同类之间互相保存指针引用的写法。所有的跨模块通知，通过 `EventBus`（基于 Qt 信号槽）进行。
```cpp
// 示例：设备收到云台更新
// 在 DeviceContext 或 JsonParser 中：
EventBus::instance()->post(DevicePtzEvent(deviceId, pan, tilt, zoom));

// 在 UI 层的 Presenter 中：
connect(EventBus::instance(), &EventBus::onDevicePtzEvent, this, [this](QString devId, double p, double t, double z) {
    if (m_currentViewedDeviceId == devId) {
        m_view->updatePtzDisplay(p, t, z);
    }
});
```

### 3.2 数学与算法逻辑剥离
不要在 `MainWindow` 里写业务算法！
*   **错误做法**: `MainWindow::pixelToGps(...)`
*   **正确做法**: `GeoCalculator::pixelToGps(...)`，通过传参和返回值获取结果，方便进行单元测试 (Unit Test)。

### 3.3 将单例裂变为实例容器
当前系统中大量使用直接实例 (`m_client`, `m_rtsp`)。为了支持多设备，必须将这些成员打包装入 `DeviceContext`。
*   `MainWindow` 不再拥有 `m_client`。
*   `MainWindow` 拥有一个 `MainPresenter`。
*   `MainPresenter` 通过 `DeviceManager::instance()->getDevice(currentId)` 找到对应的 `DeviceContext`，进而发送指令。

---

## 4. 重构实施五步走

1. **第一阶段：数学与状态剥离**  
   建立 `core` 目录，抽出 `DeviceState` 数据类和 `GeoCalculator` 算法类。
2. **第二阶段：事件总线铺设**  
   建立 `EventBus` 机制。修改底层回调（如 `TJsonClient`），让它们收到消息后不直接刷 UI，而是丢给事件总线。
3. **第三阶段：DeviceContext 封装**  
   建立 `DeviceContext` 和 `DeviceManager`，将原本散落在 MainWindow 里的 socket/rtsp 指针移入 Context 中管理。
4. **第四阶段：MVP 改造 (最痛苦的一步)**  
   新建 `MainPresenter`。删除 `MainWindow.cpp` 中几千行的网络处理逻辑和逻辑判断，所有按钮点击全转给 `Presenter`。
5. **第五阶段：多设备 UI 适配**  
   新增 `VideoGridWidget` 等组件，支持动态创建多个 `VideoWidget` 并在地图上标出多个点。

---

## 5. 编码规范提醒
*   **函数长度**: 如果一个方法超过 80 行，考虑将其剥离。
*   **魔术数字**: 避免代码中直接出现硬编码，统一定义在 `ConfigManager` 或 `constants.h` 中。
*   **指针安全**: UI 脱离底层，生命周期可能不同步。不要向 `EventBus` 发送裸指针 (raw pointer)，尽可能发送值对象 (struct) 或使用智能指针 (`std::shared_ptr`)。
## 6. 最近优化记录 (Recent Optimizations)
1. **UTF-8 with BOM 标准化 (C4828 警告修复)**:
   使用专门的 Python 脚本安全地将 `src` 下所有 `.cpp` 和 `.h` 文件转换为 **UTF-8 with BOM** 格式，彻底消除了 MSVC 编译器的 C4828 警告，并修复了由于编码截断导致的字符串未闭合 (`C3688`) 及宏定义报错。
2. **底层网络纯粹化与 UI 彻底解耦 (Phase 4 & 5)**:
   - 剥离了 `TJsonClient` 强行投递全局事件 (`EventBus`) 的逻辑，改为使用 Qt 标准信号 (`signals`)。
   - `DeviceContext` 接管了 `TJsonClient` 和 `RtspThread`，实现了与 `EventBus` 的闭环绑定，彻底打通了多设备的底层隔离。
   - `MainWindow` 不再直接调用 `tcpClient()->isConnected()` 等底层接口，全部统一收口至 `MainPresenter`，实现了真正的“哑巴视图(Dumb View)”。
3. **安全规范与智能指针 (Smart Pointers)**:
   - 遵照代码规范，`EventBus` 的设备状态载荷升级为 `std::shared_ptr<DeviceState>`，配合 `std::make_shared`，从根本上防止了事件异步分发导致的生命周期悬空指针崩溃问题。
4. **清理魔术数字 (Magic Numbers)**:
   - 将 TCP 端口 (如 `8089`)、地图可视场角扇形距离 (`4000`, `2000`) 统一提取至了 `ConfigManager` 中 (`deviceTcpPort()`, `visFovDistance()`, `irFovDistance()`)。
5. **解决多线程安全与退出时的堆损坏 (Heap Corruption)**:
   - **修复 FFmpeg SIMD 内存溢出**: 针对 `sws_scale` 在执行 SIMD 指令时要求 32-byte 内存对齐并可能会引发越界写的问题，为 `RtspThread` 分配 `m_rgbBuf` 时加入了 32 字节对齐以及 `+ 1024` 的安全内存填充 (Padding)，彻底解决了 `CRT HEAP CORRUPTION DETECTED` 报错。
   - **修复退出野线程**: 修改了 `MainWindow::~MainWindow()`，在软件销毁时调用 `DeviceManager::instance()->removeAllDevices()`，确保所有后端设备 TCP 和 FFmpeg `RtspThread` 被安全退出。
   - **解决双击切换卡顿**: 修复了 `RtspThread::openStream` 内部由于 `m_stop` 标志竞争导致的 2 秒 `wait()` 卡死 Bug。通过安全执行 `closeStream` 后同步 `wait(3000)` 平稳释放旧线程，实现了多设备树中双击设备切流的秒级顺畅体验。
